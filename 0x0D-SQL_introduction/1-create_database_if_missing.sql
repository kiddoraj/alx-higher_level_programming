-- Script to create the hbtn_0c_0 database on MySQL server

-- Connect to the MySQL server
-- Make sure to replace 'username' and 'password' with your actual MySQL credentials
mysql -u username -p password -e "CREATE DATABASE IF NOT EXISTS hbtn_0c_0;"
