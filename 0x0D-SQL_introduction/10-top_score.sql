-- Script to list all records of the table second_table

-- List all records of the second_table, displaying score and name, and ordering by score (top first)
SELECT score, name
FROM second_table
ORDER BY score DESC;
